[build]
builder = "NIXPACKS"
buildCommand = "npm run build:prod:force"

[deploy]
startCommand = "node dist/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
NODE_VERSION = "22"
