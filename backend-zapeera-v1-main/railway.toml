[build]
builder = "NIXPACKS"
buildCommand = "npm run build:prod"

[deploy]
startCommand = "NODE_ENV=production npm run start:web"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
PORT = "3000"
NODE_VERSION = "22"
